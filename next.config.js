/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'images.pexels.com', 'www.wordstream.com', 'i.ytimg.com']
    }
}

module.exports = nextConfig
