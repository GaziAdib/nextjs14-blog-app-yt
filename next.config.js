/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'images.pexels.com', 'www.wordstream.com']
    }
}

module.exports = nextConfig
